// This file is intentionally empty. The previous `ActivityTrends` widget
// (Steps + "Calories burned" line chart) was rolled into `FitnessTrends`
// in fitness_trends.dart — the calories line chart duplicated the Move
// calories bar chart, so we kept only the bars and brought Steps along.
// Leaving the file in place rather than deleting it preserves git history;
// nothing imports from it.
